import { SiglasCursoFormatadasPipe } from './siglas-curso-formatadas.pipe';

describe('SiglasCursoFormatadasPipe', () => {
  it('create an instance', () => {
    const pipe = new SiglasCursoFormatadasPipe();
    expect(pipe).toBeTruthy();
  });
});
